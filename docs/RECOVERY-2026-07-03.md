# RiptOPL functional-recovery plan — 2026-07-03

> **IMPLEMENTATION STATUS (same day):** PR-A…F are implemented on this branch and build green
> on `ps2dev/ps2dev:latest` (clang-format clean, adversarially re-reviewed):
> `fff90ace`+`af8be2d6` (PR-A settings/boot-dir resolver + typed-root guard),
> `9a055919`+`2a8a2b4f` (PR-B MMCE pacing + in-game hardening),
> `834f8d29` (PR-C VCD launch teardown), `b8055ee5` (PR-D UDPFS + lng append-only reorder),
> `90dcb223` (PR-E CI latest-primary flip), `3c8c08ce` (SMB port doc), `832cbdc4` (adversarial
> review round: 11 confirmed findings hardened — u64 timer width in the resolver waits,
> save-status masking via `configWriteChecked`, literal-slot scan preference, always-diagnosed
> launch aborts, checked toml rewrite, toggle-proof network notices, LOCALVERSION on tags/extras).
> **UPDATE 2026-07-04:** the MMCE driver-pinning item is now DONE (not deferred) — a hardware
> report of the **menu-side** MMCE cover-art freeze, plus empirical container inspection, confirmed
> the `ps2dev:latest` mmceman/sio2man desync without an A/B test: the container ships a post-refactor
> `sio2man` (single `sio2man.irx` 5241b with the `sio2man-1300/1400/2000` variant *names* as
> symlinks) while ps2sdk-ports pins `mmceman` to `v2.1.1` (predates the 2026-06-14 `cccc366`
> sio2man-compat fix). `.github/scripts/install_coherent_mmce.sh` now rebuilds the trio from
> `ps2-mmce @ db3e93f0` (has the fix; export ordinals verified to match `device-mmce.c`) over the SDK
> prebuilts in every latest CI job. Verified locally in the container: driver builds, ordinals match,
> full OPL build embeds it green. Still deferred: digest-pinning "latest", a PCSX2 smoke job, and an
> EE-side hardening of the *menu* MMCE read (unbounded `read()` with no timeout — a genuinely dead
> card can still hang; mirror the in-game bounded-wait). **The other symptoms remain HARDWARE-PENDING** — run the §8 matrix.

> **UPDATE 2026-07-05b (last recovery residual root-caused + fixed):** neutrino-hosted-on-MMCE
> launches failed in BOTH loader generations (boot.elf-revert pre-#67, black screen post-#67)
> because of a probe/load API split: OPL resolves `mmce0:/neutrino/neutrino.elf` via
> fileXio→**iomanX** (succeeds), but the child loader loaded it via SifLoadElf→rom0:LOADFILE→
> plain **ioman** — and `mmceman` registers its device with iomanX ONLY (binary import tables:
> mmceman imports `iomanx`, bdmfs_fatfs imports `ioman`, which is why USB-hosted worked). Also
> explains "worked once then never": a leftover neutrino/ folder on the card makes Auto prefer
> the unloadable card copy for MMCE-hosted games. FIX: the elfldr child now loads the target ELF
> **via fileXio (iomanX-aware) first**, SifLoadElf fallback — one load path covering
> mass/mc/mmce/pfs/smb, finally consistent with the probe.

> **UPDATE 2026-07-04 (verification round against primary sources):**
> 1. **VCD/POPSTARTER selector ([PR #66](https://github.com/NathanNeurotic/Open-PS2-Loader/pull/66)) — VERIFIED CORRECT, ready to roll.** The maintainer
>    hardware-confirmed both selector forms: `mass:/POPS/XX.<name>.ELF` for every BDMA source
>    (**including MMCE** — POPSTARTER reads MMCE via a BDMA driver variant that remounts the card
>    as `mass:`) and `smb:/POPS/SB.<name>.ELF` for SMB. Cross-checked against POPSLoader
>    (`BuildPopstarterSelectorPath` emits the same strings; `TranslateMMCEPathForPopStarter`
>    rewrites `mmce<N>:/` → `mass:/`; the BDMAssault variants all register device name `"mass"`).
>    POPSTARTER receives the selector as argv[0] with argc=1.
> 2. **Neutrino USB black-screen → OSDSYS — ROOT CAUSE CONFIRMED + FIX BUILT** (branch
>    `claude/neutrino-noreset-handoff`): ps2sdk's `LoadELFFromFileWithPartition` child loader
>    SifLoadElf()s neutrino.elf through the live mounts but then `SifIopReset`s and reloads ONLY
>    `rom0:SIO2MAN/MCMAN/MCSERV` (ee/elf-loader/src/loader/src/loader.c:131-146) — Neutrino then
>    cannot read its `-cwd` config/modules or open the `-dvd` ISO (it does all of that through the
>    LAUNCHER's mounts before its own IOP reset, neutrino main.c:294/477/728). NHDDL avoids this
>    with a vendored no-reset loader ("Modified to not reset IOP for use with NHDDL"). OPL's args
>    were verified byte-correct — only the handoff changes. Fix: vendored `elfldr/` no-reset child
>    loader (`sysLoadELFKeepIOP`) + `deinitEx` keeps BOTH the game device and the neutrino.elf
>    device mounted. NOTE: ps2sdk gained `LoadELFFromFileWithPartitionNoReset` upstream (8890d8b5,
>    2026-06-30) but NEITHER container ships it yet (latest carries a 2026-06-21 SDK) — hence the
>    vendored loader, which also fixes the OLDSDK ELF.
> 2b. **Third rolling flavour: `-woplsdk`.** wOPL's answer to the MMCE churn is building the whole
>    stack in a digest-pinned pre-churn SDK container ("use older sdk container until mmce is
>    stabilised in sdk" — KrahJohlito). Rolling now builds a third full set on that EXACT container
>    (ghcr.io/ps2homebrew/ps2homebrew@sha256:207523ad…), stock SDK MMCE drivers, no coherent-mmce
>    override: `APP_RIPTOPL-WOPLSDK/` + `RIPTOPL-*-woplsdk.ELF`. Hardware reports become a
>    three-way discriminator (latest+coherent-mmce vs wOPL-snapshot-stock vs 2025-pin). Note the
>    empirical twist that motivated it: wOPL's pinned container's sio2man/mmce trio is currently
>    sha256-IDENTICAL to ps2dev:latest stock — their immunity is whole-snapshot coherence plus a
>    menu that never streams art off the MMCE card, so the flavour isolates exactly that.
> 3. **MMCE in-game freeze — RETEST BEFORE ANY VERDICT.** No in-game freeze report exists on a
>    build containing the coherent driver: issue #56's last comment is 2026-07-02, the first
>    #64-fixed zip published 2026-07-03. A report only counts if the on-screen version is
>    **Beta-2921 or later** (2827a2a4 = merge of #64; current rolling = 2925). The "upstream
>    mmcedrv limitation" hypothesis stays UNVERIFIED until a ≥2921 freeze is reproduced.

> **UPDATE 2026-07-05 (first HW reports on the 3-flavour zip → two root causes closed):**
> - **PS1/VCD "__common" on every non-HDD device — REAL ROOT CAUSE FOUND (PR #66 was necessary
>   but not sufficient):** the stock SDK elf-loader's child CLOBBERS the target's argv[0] with
>   `<partition><filename>` and shifts the selector to argv[1]
>   (ps2sdk loader.c: `new_argv[0] = fullPath`), so POPSTARTER never saw the `XX./SB.` prefix and
>   always took its HDD route ("__common"). POPSLoader's vendored loader explicitly "preserves
>   caller args" — that's the divergence. FIX: the elfldr/ child contract is now
>   argv[0]=load-path, argv[1..]=the target's FULL argv (caller controls the target's argv[0]);
>   `sysLaunchPopstarter` hands non-HDD launches through it (selector stays argv[0], no OPL-side
>   IOP reset — POPSLoader REBOOT_IOP=0 parity); HDD/APA keeps the classic partition-context
>   loader (its HDD route is correct there).
> - **MMCE in-game (OPL core) on the latest flavour — OWNED BY OUR DRIVER REBUILD, not upstream:**
>   testers report OPL-core MMCE games broken on `-latest` but working on `-woplsdk` (same code).
>   Binary analysis: mmcedrv imports NO sio2man (drives SIO2 directly) and its SOURCE is unchanged
>   v2.1.1→db3e93f0 — our repo-Makefile rebuild (10009b vs stock 11337b) is the only in-game
>   delta, and the stock ports-built binary is field-proven (wOPL ships the byte-identical file).
>   FIX: `install_coherent_mmce.sh` now installs ONLY the rebuilt mmceman (menu pairing fix,
>   HW-confirmed) and keeps STOCK mmcedrv/mmceigr in-game.
> - **Neutrino handoff (PR #67) HW-CONFIRMED on USB** (+ gameID/per-game VMC switching restored
>   as a side effect). Residual under watch: neutrino-hosted-on-MMCE black screen (one report).

Hardware validation (2026-07-02/03) came back negative on four fronts. A six-track
investigation (each finding adversarially re-verified against the code where noted)
traced every reported symptom to concrete root causes. This document records the
findings and lays out the fix plan, ordered so the repo converges back to
"functional on `ps2dev/ps2dev:latest` as the PRIMARY target, `ps2max/dev` pinned as
the fallback".

Reported symptoms:

1. Saving settings to the boot directory (cwd) isn't working.
2. UDPFS is not working.
3. Internal exFAT HDD (`ata`, should normalize to `massN:`) doesn't launch VCD/POPSTARTER.
4. MMCE lists games but freezes after the PS2 logo ([issue #56](https://github.com/NathanNeurotic/Open-PS2-Loader/issues/56)).

## TL;DR — symptom → root cause → fix

| Symptom | Root cause(s) | Verified | Predicted on -OLDSDK ELF |
|---|---|---|---|
| 1. Settings cwd | (a) Config is read **before any boot-device driver is loaded** and the legacy fallback is dead code (gated on `gBootDir` empty, which never happens). (b) exFAT-HDD boots additionally hand an `ata0:` launch identity that is never openable, and `gEnableBdmHDD` lives in the unreachable config (circular). | CONFIRMED ×3 | Same failure (pure code bug) |
| 2. UDPFS | Stack of day-one gaps: connect-once with **no retry/reconnect** in `udpfs_ioman` (the default Files mode); OPL lists only `CD/`+`DVD/` while docs say "flat folder of ISOs"; Neutrino tomls hardcode `ip=192.168.1.10` vs OPL default `192.168.0.10`; retired-UDPBD users silently migrated to a wire-incompatible server; docs promise a bundled `udpfs_server.py` that isn't in the repo or zip. | CONFIRMED ×4 | Same failure (repo modules, day-one bugs) |
| 3. exFAT VCD/POPSTARTER | (a) Launch `deinit()` runs `hddShutdown` → **DEV9 powered off** before OPL re-opens POPSTARTER.ELF from the ATA-backed `massN:` mount (HW-only; PCSX2 masks it). (b) On `ps2dev:latest` only: ps2sdk's Feb-2026 "typed devices" register REAL `ata0:`/`usb0:` filesystems, activating OPL's typed-root branch → paths resolve to `ata0:` instead of `massN:`. (c) POPSTARTER picker "HDD (APA)" leg resolves onto `pfs0:` which the same deinit unmounts. (d) Silent BDMA fallback **wipes a working equipped driver pair** when the `.ata` variant files are missing. | (a) PLAUSIBLE (HW-only link), (b) investigator-confirmed, (c)(d) CONFIRMED | (b) fixed on OLDSDK; (a)(c)(d) same on both |
| 4. MMCE freeze | (0) **Provenance**: issue #56's build (2896, 2026-07-02) PREDATES the first PR #57-fixed zip (2026-07-03 ~04:04 UTC) — the report was taken on the known-broken rolling driver. (a) HEAD still ships SIO2 pacing defaults `0` wait-cycles + alarms **OFF** (alarms-off removes the only timeout on the driver's WaitEventFlag → any lost handshake = permanent hang); the 5/ON restore (`ee0d3bb2`) was never merged. (b) `ps2dev:latest`'s mmcedrv/sio2man pairing is broken for in-game use (see §4). (c) `device-mmce.c` has an unbounded `while(1)` on `mmcedrv_get_size` + a u32-truncated s64 return that turns errors into freezes. | (0) PLAUSIBLE-verified, (a)(c) investigator-confirmed, (b) two independent tracks agree on conclusion | (b) predicted fixed on OLDSDK; (a)(c) same on both |

**Two zero-code actions before anything else:**

1. **Re-download the current rolling zip** (version must contain `0d2ed25` or later) and
   retest MMCE — the tested build had the pre-PR-#57 broken driver.
2. **Run the A/B matrix on `APP_RIPTOPL-OLDSDK/RIPTOPL.ELF`** (same zip, same code,
   pinned SDK). Predictions: MMCE-launch and exFAT-VCD improve on OLDSDK
   (SDK-delta component); settings-save and UDPFS fail identically on both (code bugs).
   Every latest-only failure routes to the SDK track; every both-fail routes to the code track.

---

## 1. Settings / cwd config (symptom 1)

### Root causes (all CONFIRMED by independent re-trace)

- **Read-before-drivers, on every non-MC boot.** `sysReset()`
  ([system.c:189](../src/system.c)) loads only
  iomanx/filexio/sio2man/mcman/mcserv/padman/usbd/…— no `bdm.irx`, no
  `bdmfs_fatfs`, no block drivers, no `mmceman`. `init()` then does
  `configInit(gBootDir)` → synchronous `_loadConfig()` ([opl.c:2316/2348](../src/opl.c))
  **before** any device support initializes. So `open("mass0:/APPS/settings_riptopl.cfg")`
  fails on a freshly reset IOP; defaults load; an immediate save fails
  ("Error saving settings"). A save after the user opens the device tab *succeeds* —
  but the next boot can't read it back, so settings never persist. PCSX2 masked this
  (`host:` is always mounted), which is how 8d6232f2 shipped "validated".
- **Dead fallback.** The legacy discovery / alternate-save gates
  ([opl.c:1217](../src/opl.c), [opl.c:1641](../src/opl.c)) require `gBootDir[0]=='\0'`,
  which essentially never happens (argv[0] is set by every real launcher, getcwd backup
  otherwise). `tryAlternateDevice` — the code that knows how to force-load drivers — is
  unreachable.
- **exFAT-HDD boots are doubly broken.** Form A: `argv[0]="ata0:/..."` is a
  launch-binding identity — never an openable filesystem — so read AND save fail
  forever. Form B: `argv[0]="mass0:/..."` needs `ata`-backed `massN:` mounted, which
  needs `hddLoadModules()` via `gEnableBdmHDD=1` — a flag stored in the unreachable
  config (circular; fork default is OFF, [opl.c:2288](../src/opl.c)). With a USB stick
  also attached, a save can silently land on the *stick*.
- **setBootDir edge cases** (medium): uLE APA-HDD paths (`hdd0:__common:pfs:/...`),
  backslash paths, and `host:C:\...` all produce unopenable or wrong boot dirs with no
  fallback.

### Fix design (verified against the code; branch `fix/bootdir-resolve-massn` @ `a245b607` is a start but INSUFFICIENT alone)

The existing unmerged commit `a245b607` resolves `ata0:`-style identities to `massN:`
at deferred-config-load time — correct direction, but it does **not** fix the
read-before-drivers hole (a `mass0:` boot dir is left untouched and still unreadable at
boot), and on `ps2dev:latest` the typed-devices delta (§5) would make
`bdmGetDeviceRootByType` return `ata0:` — a no-op rewrite. Full design:

1. **Classify** the boot prefix right after `setBootDir` (strip trailing digits from the
   `<device>:` token): `mc/host` → ready; `mmce` → needs mmceLoadModules;
   `mass` → needs BDM (type unknown); `usb/ata/sdc/mx4sio/sd/ilink` → needs BDM (typed);
   `hdd0:...pfs...` multi-colon and unknown → UNRESOLVED. Normalize `\` → `/`.
2. **Bring-up before the first read, after `ioInit`**: place it after `ioInit()`
   (opl.c:2322) and before `_loadConfig()` (opl.c:2348) — NOT before `configInit`,
   because `bdmLoadModules` queues block drivers via `ioPutRequest`, which silently
   drops requests before `ioInit` registers the handler (ioman.c:227). Load
   `bdmLoadModules()` + the classified block driver synchronously, **ignoring
   `gEnable*` defaults** (for ATA: `hddLoadModules()`; precedent: `miniInit`
   opl.c:2405-2413 and `checkLoadConfigBDMHDD` opl.c:1091).
3. **Resolve** with a bounded poll (~3 s USB / ~10 s ATA spin-up; factor the
   autoLaunchBDMGame loop at opl.c:2540 into a helper — its current bounds are much
   shorter): typed identity → first `massN` whose driver matches
   (`USBMASS_IOCTL_GET_DRIVERNAME`/`GET_DEVICE_NUMBER`); untyped `massN` → verify by
   probing `<candidate>:/<folder>/<elf-basename>` so launcher-vs-OPL slot renumbering
   can't land settings on the wrong stick. Rewrite only the device prefix
   (`ata0:/RIPTOPL` → `mass1:/RIPTOPL`).
4. **UNRESOLVED after timeout** → clear `gBootDir` (or add a `gBootDirResolved` flag
   consumed by the opl.c:1217/1641 gates) so legacy discovery re-arms — restores
   pre-8d6232f2 behavior for exotic launchers instead of failing forever.
5. **Reconcile after load**: if the loaded config disables the boot device's driver
   (`gEnableBdmHDD=0` while booted from ATA), force it on and `configSetInt` it
   (precedent opl.c:1097-1099).
6. **Save-side**: on write failure with a BDM boot dir, re-resolve once (hotplug may
   have renumbered slots) and retry. Never fall back to another device (PR #59 was
   rejected for that).
7. **Typed-root guard** (§5 dependency): the resolver must prefer legacy `massN:` roots
   even when latest-SDK typed devices make `ata0:` openable.

Bonus: the resolved `gBootDir` automatically fixes the POPSTARTER "Default (cwd)" tier
([vcdsupport.c:193](../src/vcdsupport.c)).

Also worth folding in (medium/low, same area): `configWriteMulti` counts unmodified
sets as successes (save can report success when the master write failed);
`guiHandleDeferedIO`'s 30 s bound counts FIFO **queue** time, so a save queued behind a
long list-refresh errors spuriously (start the clock when the request starts, or drain
list work like art is drained).

### Tests
HW matrix: boot from MC, MMCE, USB, MX4SIO, internal exFAT HDD (both argv[0] forms),
USB+HDD simultaneously → save a distinctive setting, power-cycle, verify it persisted
and `settings_riptopl.cfg` exists ONLY in the boot folder. PCSX2 regression: `host:`
boot still saves. MC/MMCE boots must not gain any BDM wait.

---

## 2. UDPFS (symptom 2)

All day-one design gaps (dd8aeddc-era); none SDK-sensitive; most PCSX2-testable.
In likely order of user impact:

1. **Connect-once, no reconnect — CRITICAL, CONFIRMED.** The Files variant (the
   default sub-mode) connects inside `AddDrv` at module load with a ~5 s discovery
   window racing PHY autonegotiation (smap needs a hard minimum ~3 s), and nothing ever
   retries: `udpfs_init_dev` is the only caller of `udpfs_core_init`
   ([udpfs_ioman.c:76-93](../modules/network/udpfs_ioman/src/udpfs_ioman.c)), and any
   later recv timeout latches `STATE_DISCONNECTED` forever. The EE side latches
   `udpfsIomanModLoaded` on *load*, not connect
   ([udpfssupport.c:53](../src/udpfssupport.c)). The reconnect watchdog 1ed7a176 added
   to `udpfs_bd` was never mirrored into `udpfs_ioman`. **Fix:** register the device
   unconditionally; lazy-connect/reconnect on open/dopen/getstat (the bound UDPRDMA
   socket is reusable — socket-once is preserved), or clone the udpfs_bd watchdog.
2. **CD/DVD layout vs "flat folder" docs — HIGH, CONFIRMED.** OPL scans only
   `udpfs:/CD` + `udpfs:/DVD` (`sbReadList`), while docs/NEUTRINO.md:137/165 tells
   Files users to serve a **flat folder of loose ISOs** → guaranteed empty list.
   **Fix decision needed:** (a) fix the docs (CD/+DVD/ under the served root — verify
   udpfsd handles subdir opens first), or (b) true flat-share support, which requires
   udpfs-aware path construction end-to-end (`sbCreatePath` inserts `CD/`/`DVD/`
   unconditionally, so a naive root-scan would list games that then fail to launch).
3. **Launch-time IP mismatch — HIGH, CONFIRMED.** OPL's own chain uses `ps2_ip`
   (browse works), but Neutrino reads `ip=192.168.1.10` hardcoded in
   `bsd-udpfs.toml`/`bsd-udpfsbd.toml`. Boot fails whenever the configured PS2 IP ≠
   192.168.1.10 with an unedited toml — including the out-of-box default
   (192.168.0.10). **Fix:** rewrite/generate the active toml's `ip=` from `ps2_ip` at
   launch (the Neutrino-hosting device stays mounted via UNMOUNT_EXCEPTION); minimum: warn on mismatch.
4. **UDPBD retirement breaks working setups silently — HIGH, CONFIRMED.** Saved UDPBD
   configs fold to UDPFSBD (UDPRDMA, port 0xF5F6) but the user's udpbd-server
   (SUDPBDv2, 0xBDBD) never answers; the tab just never appears. **Fix:** one-time
   in-app notice when the fold fires ("switch PC server to `udpfsd -b`").
5. **Missing PC server — MEDIUM, CONFIRMED.** Docs + release notes reference a bundled
   `udpfs_server.py`; `pc/` has none and the zip packages none (and release notes still
   call UDPBD "the default"). **Fix:** vendor a server into `pc/udpfsserver/` (package
   like PC-SMB-Server) or strip every "bundled" claim; fix the release-notes heredoc.
6. **Asymmetric NIC guard — MEDIUM.** `ethLoadModules` doesn't check
   `udpfsGetModulesLoaded()` (the other two directions are symmetric) → in-session
   UDPFS→SMB switch can double-drive the SMAP. One-line guard.
7. **DHCP default + UDP transport — MEDIUM.** ministack has no DHCP; `ps2_ip` default
   is used silently when IP Type = DHCP (the default). Warn at tab-entry/boot, not only
   on the protocol-change dialog.
8. **Enum renumbering (2/4 swap between a5229759 and f4ce126b) — configs saved by that
   ~21-min window misread today** (saved UDPBD=2 now selects Files). Low exposure;
   at most a sanity-check against legacy shadow keys *before* first re-save.
9. `udpfs_bd` tears down the block device from inside its own read/write callback on IO
   failure (re-entrant teardown; possible slot renumbering). Defer to the watchdog thread.

Cleared: the bundled Neutrino (v1.8.0-47) **does** ship `bsd-udpfs.toml` +
`udpfs_fhi/udpfs_ioman/udpfs_bd` modules (verified against the actual release asset) —
still add a packaging assert since the bundle is re-fetched every publish.

---

## 3. Internal exFAT HDD → VCD/POPSTARTER (symptom 3)

- **DEV9 power-off on the launch path — CRITICAL (PLAUSIBLE; final link is HW-only).**
  `bdmLaunchVcd` resolves the ELF, then `deinit(UNMOUNT_EXCEPTION, <bdm mode>)` →
  `moduleCleanup` calls `itemShutdown` for every non-selected support *without* the
  exception flag → `hddShutdown` ([hddsupport.c:1025](../src/hddsupport.c)) decrements
  dev9 and powers the DEV9/ATA bus **off** → `sysLaunchPopstarter` then `open()`s
  `mass<N>:POPS/POPSTARTER.ELF` on a dead bus → elf-loader returns into deinit'd OPL =
  black screen. ISO launches are immune (embedded ee_core, no post-deinit read).
  PCSX2 masks it (emulated DEV9 power-off is inert). Refcount defect is sharper than
  one bad call: dev9InitCount only ever reaches 1 from the HDD side while hddShutdown
  decrements per call. **Fix options:** pair `sysShutdownDev9()` with
  `hddModulesLoadCount` reaching 0 (minimal), and/or never power DEV9 down on the
  launch path at all (POPSTARTER/ee_core reset the IOP anyway) — reserve power-off for
  real poweroff/exit.
- **Typed-devices SDK delta — CRITICAL on `ps2dev:latest` only (investigator-confirmed).**
  ps2sdk 2026-02-26 (`3061dbe2`, `5d64b0eb`) gives block devices a `path` and registers
  REAL `ata0:`/`usb0:`/`mx4sio0:`/`ilink0:` iomanX filesystems. RiptOPL's
  `bdmProbeTypedDeviceRoot` branch — previously self-defeating by design ("Dopen always
  fails") — now *succeeds* on latest builds, so `bdmGetDeviceRootByType(BDM_TYPE_ATA)`
  returns `ata0:/` and VCD/POPSTARTER/Neutrino paths get built on `ata0:`, violating
  the fork's own massN-throughout rule. **Fix:** prefer the legacy `mass<slot>:` root
  unconditionally in `bdmResolveDeviceRoot` (or gate typed roots behind a flag);
  keep launch-binding tokens as-is. NOTE: this invalidates the standing "typed roots
  never open" invariant — treat it as SDK-version-dependent from now on.
- **Picker "HDD (APA)" leg — HIGH, CONFIRMED.** `POPS_DEV_APA_HDD` resolves
  `pfs0:/POPS/POPSTARTER.ELF`; a launch from any non-HDD page then unmounts `pfs0:` in
  deinit before the ELF is read. **Fix:** allow deinit to except the ELF-holding
  device's mode too, or make hddShutdown honor the exception context, or skip the
  `pfs0:` leg for non-HDD launches (fall through to Default).
- **Silent BDMA equip fallback wipes working pairs — HIGH, CONFIRMED.** On ANY equip
  failure (missing `.ata` files, full MC, IO error), `vcdEnsureBdmaForLaunch` silently
  downgrades to FAT32, **deleting** `mc?:/POPSTARTER/usbd.irx`+`usbhdfsd.irx` (a
  working usbexfat pair from a previous session is collateral). No message on the
  launch path (settings-screen equip does surface diagnostics). **Fix:** surface the
  -4/-2/-3 diagnostic + abort the launch; never destroy an equipped pair as fallback.
- **gBDMPrefix desync — MEDIUM.** With a BDM prefix set, OPL scans
  `mass0:<prefix>/POPS` but POPSTARTER always reads `<root>/POPS` → VCDs list, never
  boot. Scan device-root POPS regardless of prefix (or annotate).
- **Issue #56 open sub-report:** PS1-from-MMCE launch shows POPSTARTER debug
  `Cannot mount __common to pfs1` — POPSTARTER taking its APA/HDD path for an
  MMCE-hosted VCD. Needs one targeted trace of `mmceLaunchVcd` + which POPSTARTER
  variant the zip's `POPSTARTER/` folder actually contains (it's packaged from
  `popsmb/`, the SMB variant). Not yet root-caused.

---

## 4. MMCE (symptom 4)

**Order of operations matters here:**

1. **Provenance (verified).** The first zip containing PR #57's driver fix published
   2026-07-03 ~04:04 UTC. Issue #56 (created 2026-07-02, build 2896) tested the
   known-broken rolling driver. → Ask the reporter to re-test on the current zip
   (version string must contain `0d2ed25`+) before treating the OPL-core freeze as live.
2. **Land the pacing defaults — CRITICAL, investigator-confirmed.** HEAD ships
   `gMMCEAckWaitCycles = 0; gMMCEUseAlarms = 0` ([opl.c:2282](../src/opl.c)) —
   overriding the driver's safe internal defaults (5/ON). With alarms OFF the driver's
   `WaitEventFlag` has **no timeout path at all**; the fork's own hint strings warn
   this can freeze. Build 2421 (last HW-good) shipped 5/ON; the restore commit
   `ee0d3bb2` sits on unmerged branch `fix/mmce-sio2-pacing-default` (PR #55 was closed
   while the driver download was the suspected root cause — the evidence now says land
   it). Include a migration for configs that persisted 0/0 (saved values override new
   defaults on load). Note the vicious loop: symptom 1 currently prevents even the
   manual 5/ON mitigation from persisting.
3. **Latest-SDK in-game pairing — CRITICAL.** Two investigation tracks agree the
   `ps2dev:latest` container's mmcedrv is the wrong pairing for in-game use, though
   they dispute the exact generation (empirical container extraction: post-refactor
   family built 2026-06-21, whose sio2man hook whitelists only IRX versions
   {1.2, 2.7} and whose exclusivity lock is a no-op against the common in-game Sony
   XSIO2MAN v1.2; ps2sdk-ports tracing: a stale v2.1.1 pin predating the June-14
   sio2man-updates fix). Either way: menu-side works (ps2sdk's sio2man registers both
   v2.7 and a v1.2 compat table), in-game is exposed (ee_core reloads mmcedrv on every
   IOP reset; the game's own Sony sio2man comes from its IOPRP —
   [iopmgr.c:148](../ee_core/src/iopmgr.c)). The pinned OLDSDK ELF carries the
   pre-refactor driver that semaphore-gates any sio2man > 1.1 (the HW-good 2421-era
   family). **Fix:** for latest builds, stop trusting the container's prebuilt — build
   mmceman/mmcedrv/mmceigr from a repo-pinned ps2-mmce commit ≥ 2026-06-15 in the
   latest job (or vendor the three IRX), and settle the provenance question with the
   IRX manifest (§5). **Discriminator:** same game, same settings, `APP_RIPTOPL` vs
   `APP_RIPTOPL-OLDSDK`.
4. **Harden `device-mmce.c` — MEDIUM, confirmed.** Unbounded
   `while (1) { iso_size = fp_mmcedrv_get_size(...); }` on the first FS access after
   every IOP reboot (i.e. right after the PS2 logo), plus the prototype truncates the
   driver's `s64` (−1 error passes the `> 0` check as 0xFFFFFFFF). Bound the loop
   (~5 s → SCECdErREAD), fix the prototype, and validate the raw export-ordinal binding
   (`info.exports[4..9]`, settings IDs 0/1/2) at build time (objdump check in CI) —
   it's correct today on both containers (verified by binary export-table parse) but
   permanently fragile.
5. **Issue #56 residuals to re-test after 1–4:** Neutrino-from-MMCE → OSDMENU
   (issue #51 class), USB+Neutrino freeze with gameID reaching the card, PS1-from-MMCE
   `__common` mount error (§3). Re-collect all on a post-fix zip before further work.

---

## 5. Latest-SDK-primary: what changed and the CI restructure

SDK fingerprints: pinned `ps2max/dev:v20250725-2` = 2025-07-25 SDK (EE GCC 15.1 /
IOP GCC 14.2); `ps2dev:latest` probed 2026-07-03 = EE+IOP GCC 15.2. Deltas that map to
symptoms: **typed BDM devices** (→ §3) and the **mmce pairing** (→ §4). Also on the
watchlist (HW-untested on latest): FatFs R0.15 → R0.16-rolling in `bdmfs_fatfs`
(exFAT write-integrity — test on a scratch stick before trusting the HDD; also
re-weighs the old exfat-config-corruption theory), lwIP 2.0.3 → 2.2.1 + smap rework
(app-side SMB), atad probing changes (May/June 2026), srxfixup changes (2026-03-06)
affecting all repo-built IOP modules.

Component classes (full matrix in the investigation record): (a) ~21 app-side + ~10
in-game prebuilt IRX consumed from `$(PS2SDK)/iop/irx` — silently change with the
container; the in-game subset (udnl, eesync, usbd_mini, *_bd_mini, smbman, mmcedrv,
mmceigr) is highest-risk; (b) vendored binaries (pops/, popsmb/, IOPRP.img) —
container-independent; (c) build/package-time downloads — langs, lwNBD (pinned),
**neutrino release re-fetched every publish** (moving target); (d) repo-built EE/IOP
code — self-consistent per container but inherits toolchain deltas.

CI plan (do after the code fixes above so green means something):

1. **Flip gating** in rolling-release.yml: the latest job becomes required and
   produces `APP_RIPTOPL`; the pinned job gets `continue-on-error` and produces
   `APP_RIPTOPL-OLDSDK`. If latest fails, **fail the publish** (or publish a clearly
   labeled OLDSDK-only zip) — never silently swap roles (today's behavior).
2. **Pin "latest" by digest**: a scheduled SDK-bump workflow resolves
   `ps2dev/ps2dev:latest` to a sha256 digest, runs probe+build+smoke, and PRs the
   digest bump. Release builds use the digest → reproducible "latest".
3. **Fix build identity**: on the Alpine latest jobs, git ops run before `apk add git`
   → version collapses to `v1.2.0-Beta-2-dirty` (exactly what issue #56 reported).
   Reorder, and embed the toolchain (`-latest` / `-oldsdk`) in `OPL_VERSION` so HW
   reports self-identify.
4. **IRX provenance manifest**: sha256 every consumed `$(PS2SDK)/iop/irx` file + every
   repo-built .irx; publish next to SHA256SUMS.txt; CI-diff vs the previous release
   (catches the next mmceman-class silent swap). Extend the existing migration probe.
5. **PCSX2 smoke job**: boot each packaged ELF headless to the menu (labs/opltestiso
   pattern) + static opl.map check that expected `*_irx` blobs are embedded.
6. **MMCE special-case** until the pairing question is settled: latest job builds the
   mmce trio from a pinned ps2-mmce ref instead of copying container prebuilts.
7. Delete the fully-redundant `compilation-ps2dev-latest.yml`; keep the migration
   probe (extended with hashes); assert `config/bsd-udpfs.toml` exists in the fetched
   Neutrino bundle.

---

## 6. Hidden problems found (fix opportunistically, several are update-breakers)

| # | Problem | Fix |
|---|---|---|
| H1 | **lng string-ID misalignment** — four fork strings inserted mid-list in `lng_tmpl/_base.yml` shift positional IDs for every stale external language file (wrong labels everywhere post-update; users can misread destructive options) | Move the four labels to the END before the next release; adopt append-only rule; longer-term add a count/version header to the .lng format |
| H2 | SMB default-port churn 445→1000→1024→1111 desyncs saved configs from the updated bundled server (both directions) | Stop churning; README/release-note migration table ("saved before X → set port 1111") |
| H3 | UDPBD retirement + wire-incompatible server (also §2.4) | One-time migration notice |
| H4 | Deferred-IO 30 s bound counts queue time → spurious save errors during list refresh | Stamp request-start; drain list work like art |
| H5 | `udpfs_bd` in-callback teardown (§2.9) | Defer to watchdog |
| H6 | MMCE cross-device GameID wait cut 7.5 s→3 s can resurface issue-#50 on slow cards | Raise budget back (poll-first keeps it cheap) |
| H7 | Stale release-notes text: "bundled udpfs_server.py", "default UDPBD protocol" | Fix the heredoc in rolling-release.yml |
| H8 | [Issue #54](https://github.com/NathanNeurotic/Open-PS2-Loader/issues/54): .tar covers "don't work" — toggle WAS in the reporter's build (191fc8ee, default OFF, Display Settings); either discoverability or a real defect; also favourites info page missing fields | Verify toggle function on HW; improve labeling; check favourites info-page parity |

Cleared (checked, no regression found): config write path is pure POSIX (no fileXio);
7c24f905 legacy-name migration symmetric; DS5 addition didn't touch shared DS3/DS4
paths in a breaking way (per sweep); disc-badge decoupling; MX4SIO warn.

---

## 7. Execution order

| PR | Content | Unblocks |
|---|---|---|
| **PR-A** | Boot-dir resolver + early device bring-up + legacy-discovery re-arm (§1, supersedes/extends `a245b607`) + typed-root massN preference (§3b — small, protects both SDKs) | Settings persist everywhere → all other mitigations become persistent; POPSTARTER Default(cwd) fixed free |
| **PR-B** | MMCE: land 5/ON pacing defaults (+ 0/0 config migration) + bounded `device-mmce.c` wait + s64 prototype fix | Symptom 4 on both ELFs |
| **PR-C** | VCD launch path: DEV9 keep-alive on launch deinit + APA-picker safety + BDMA equip diagnostics (no destructive fallback) + gBDMPrefix scan fix | Symptom 3 on both ELFs |
| **PR-D** | UDPFS: udpfs_ioman reconnect + layout decision (docs or flat-scan) + toml `ip=` rewrite + NIC-guard symmetry + DHCP warning + UDPBD migration notice + server packaging/docs | Symptom 2 |
| **PR-E** | CI: identity fix + latest-primary flip + digest pinning + IRX manifest + smoke + mmce pinned-ref build + Neutrino bundle assert + release-notes cleanup | Latest-SDK-primary goal, with guardrails |
| **PR-F** | Polish: lng append-only reorder, H2/H4/H6/H8, docs sweep (NEUTRINO.md, VCD.md, README port table) | Update-safety |

PR-A first (everything else's mitigations depend on settings persisting). PR-B/C/D are
independent of each other and can proceed in parallel. PR-E after B/C so "latest
primary" flips on a build that's expected to pass hardware. Each PR: build both
containers via the local Docker recipe, clang-format, then the §8 matrix items it touches.

## 8. Hardware validation matrix (run per fix-wave, both ELFs)

| Test | Device | ELF(s) | Validates |
|---|---|---|---|
| Save setting → power-cycle → persisted; cfg only in boot folder | MC, MMCE, USB, MX4SIO, exFAT HDD, USB+HDD | both | §1 |
| MMCE: launch 3 games incl. one known-freezer | MMCE | **both** (A/B is the discriminator) | §4 |
| VCD from exFAT HDD (APA support Auto AND Off; picker Default AND HDD(APA); with+without `.ata` files in POPS) | exFAT HDD | both | §3 |
| UDPFS Files: server started AFTER console; flat vs CD/DVD layout; launch with default IP | ETH | one (not SDK-sensitive) | §2 |
| SMB list + boot (port 1111 end-to-end) | ETH | both (lwIP delta on latest) | §5 watchlist |
| exFAT write-integrity loop on scratch USB stick, then PC-side fsck | USB | latest esp. | FatFs 0.16 |
| Old lang file + new ELF → labels correct | any | one | H1 |

---

*Investigation: 6 parallel deep-dives + adversarial verification (34 agents, ~4.1M
tokens), 2026-07-03. Verification legend: CONFIRMED = independently re-derived from
code by a second agent; investigator-confirmed = fully traced by the finder, verifier
pass cut short by session limits; PLAUSIBLE = holds in code, final link needs hardware.*
